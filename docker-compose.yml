version: "3"

services:
  libram-api-mcp:
    build:
      context: .
      network: host
    image: libram-api-mcp:latest
    environment:
      - HOST:0.0.0.0
      - LIBRAM_DB
      - LIBRAM_SCHEDULER_MAX_RETRIES
      - LIBRAM_SCHEDULER_RETRY_DELAY_SECONDS
      - LIBRAM_SCHEDULER_THREADS
      - LIBRAM_SCHEDULER_MAX_TASKS_PER_DATASOURCE
      - LIBRAM_SCHEDULER_POLL_INTERVAL_SECONDS
      - LIBRAM_SCHEDULER_POLL_JITTER_SECONDS
      - LIBRAM_SNAPSHOT_SCHEDULER_THREADS
      - LIBRAM_SNAPSHOT_POLL_INTERVAL_SECONDS
      - LIBRAM_SNAPSHOT_LEASE_SECONDS
      - LIBRAM_SNAPSHOT_RETRY_DELAY_SECONDS
      - LIBRAM_SNAPSHOT_MAX_BACKOFF_SECONDS
      - LIBRAM_SNAPSHOT_RPC_CONCURRENCY
    cpus: 0.5
    mem_limit: 512mb
    network_mode: host
    user: 1000:1000
    privileged: false
    security_opt:
      - 'no-new-privileges:true'
